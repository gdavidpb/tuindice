package com.gdavidpb.tuindice.base.presentation.navigation

/**
 * Marks a [Destination] rendered as a dialog scene: the shell hides the top-bar
 * back button while one is on top of the back stack.
 */
interface DialogDestination
