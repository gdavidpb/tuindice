# After editing this file, run ./gradlew syncAppVersion and commit iosApp/Config/Version.xcconfig
# together with this change — verifyAppVersionSync fails preflight otherwise (caught late, since
# it only runs as part of PR preflight/certification, not on a plain local build).
versionName=6.4.2
androidVersionCode=63
iosBuildNumber=51
